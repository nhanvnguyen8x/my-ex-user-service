apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-ex-user-service
  labels:
    app: my-ex-user-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: my-ex-user-service
  template:
    metadata:
      labels:
        app: my-ex-user-service
    spec:
      containers:
        - name: user-service
          image: my-ex-user-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3002
          env:
            - name: PORT
              value: "3002"
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: user-service-config
                  key: DB_HOST
            - name: DB_PORT
              valueFrom:
                configMapKeyRef:
                  name: user-service-config
                  key: DB_PORT
            - name: DB_NAME
              valueFrom:
                configMapKeyRef:
                  name: user-service-config
                  key: DB_NAME
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: user-service-db
                  key: username
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: user-service-db
                  key: password
          livenessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 3002
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
data:
  DB_HOST: "user-db"
  DB_PORT: "5432"
  DB_NAME: "user_db"
---
apiVersion: v1
kind: Secret
metadata:
  name: user-service-db
type: Opaque
stringData:
  username: postgres
  password: postgres
